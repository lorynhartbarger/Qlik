/*This project takes customer information and maps to a purchase status
The code uses subfield, mid, and index to normalize the store name to map to the purchase status information for the right store */

Load
  FirstName,
  LastName,
  FirstName&' '&LastName as "Customer Name",
  Age,
  Gender,
  Email,
  Subfield(Mid("StoreLocation",Index("StoreLocation,')')+1),')',1) as Store,
  StoreLocation
From filepath;
